version: '3.9'
services:
  web:
    build: .
    container_name: farmsetu_weather_web
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
      - SECRET_KEY=change-me-compose
    volumes:
      - .:/app
    restart: unless-stopped
